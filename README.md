# user-app

This repo is generated by `scripts/push-apps.mjs` in the Wheewise monorepo. `packages/` is a flattened copy of the monorepo's shared code — don't hand-edit it here, it will drift from the other two apps. Change the source in `apps/user-app` or `packages/*` and re-run the script instead.
